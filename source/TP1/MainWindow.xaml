<Window x:Class="TP1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TP1"
        mc:Ignorable="d"
        x:Name="root"
        Title="MainWindow" Height="450" Width="800">
    <Grid Background="white" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Ellipse Fill="{Binding StatusColor}" Height="30" Width="30"/>
        <TextBlock Grid.Row="2" x:Name="StatusText" Text="{Binding StatusText}"/>
        <Viewbox Grid.Row="3" HorizontalAlignment="Center">
            <Image Name="theImage" Stretch="UniformToFill" />
        </Viewbox>

    </Grid>
</Window>
