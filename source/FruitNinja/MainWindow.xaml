<Window x:Class="FruitNinja.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FruitNinja"
        xmlns:model="clr-namespace:Model;assembly=Model"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Image Name="theImage" Stretch="UniformToFill" Source="{Binding GameViewModel.Bitmap}" />
        <Canvas x:Name="myCanvas"/>
        <TextBlock Text="{Binding GameViewModel.Score}" HorizontalAlignment="Left" Margin="50,20,0,0"/>
        <TextBlock Text="{Binding GameViewModel.Timer}" HorizontalAlignment="Right" Margin="0,20,50,0"/>
    </Grid>
</Window>
