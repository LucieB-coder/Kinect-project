<Window x:Class="FruitNinja.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FruitNinja"
        xmlns:model="clr-namespace:Model;assembly=Model"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Start Game" Background="LightGray" BorderBrush="Black" Command="{Binding GameViewModel.StartGameCommand}" Height="20" VerticalAlignment="Top" Width="70"/>

        <Grid Grid.Row="1">
            <Image Name="theImage" Stretch="UniformToFill" Source="{Binding GameViewModel.Bitmap}" />
            <Canvas x:Name="myCanvas">
            </Canvas>
        </Grid>
        
    </Grid>
</Window>
